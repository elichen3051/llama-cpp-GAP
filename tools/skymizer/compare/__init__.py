# The paired-comparison engine shared by saved_metrics_paired_compare.py and
# the power/planning tools: contracts (constants + exceptions), student_t,
# inference (CI construction), tokens (ladders/strata), engine
# (compare_items), render (markdown/JSON), cli_common (argparse/validation
# scaffolding).
#
# Importing this package has no side effects on the process environment.
